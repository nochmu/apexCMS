create or replace PROCEDURE p(p_path varchar2, p_status OUT number, p_forward IN OUT varchar2, p_location OUT varchar2) IS
BEGIN

   -- IF p_path = 'l' THEN
   --    p_status  := 303;
   --    p_forward := 'k';

   -- ELSE
   --    main_pkg.page(p_path);
   -- END IF;

null;
END p;
/
